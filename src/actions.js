export const changeCity = (city) => ({type: `CITY_CHANGE`, city});
export const loadOffers = (offers) => ({type: `LOAD_OFFERS`, offers});
export const loadCitiesList = (list) => ({type: `LOAD_CITIES_LIST`, list});
